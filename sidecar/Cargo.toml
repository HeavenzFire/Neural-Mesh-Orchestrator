[package]
name = "sidecar"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "sidecar-api"
path = "src/main.rs"

[dependencies]
quantizer-core = { path = "../quantizer-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = 3
lto = true
